import { Icon } from "@mui/material";

export default function ZGoogleIcon() {
    return (
        <Icon>
            <img src="/images/googleIcon.svg" alt="" width={16} height={16} style={{position: 'relative', top: -6}}/>
        </Icon>
    )
}